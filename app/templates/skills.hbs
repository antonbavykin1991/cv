{{page-title "Skills"}}

<div class="flex-1 container mx-auto">
  <div class="sm:p-12 sm:m-12">
    <h1 class="text-3xl text-center mb-8">Professional Skills</h1>
    <div class="flex flex-col flex-wrap bg-white dark:bg-gray-700 p-12">
      <div class="flex flex-col lg:flex-row flex-1">
        <div class="flex flex-col flex-1 lg:mr-4 mb-4">
          <span class="text-sm mb-2 font-bold">Javascript</span>
          <ProgressBar @title="Javascript" @value={{95}} />
        </div>

        <div class="flex flex-col flex-1 lg:ml-4 mb-4">
          <span class="text-sm mb-2 font-bold">HTML</span>
          <ProgressBar @title="Javascript" @value={{85}} />
        </div>
      </div>

      <div class="flex flex-col lg:flex-row flex-1">
        <div class="flex flex-col flex-1 lg:mr-4 mb-4">
          <span class="text-sm mb-2 font-bold">CSS</span>
          <ProgressBar @title="Javascript" @value={{70}} />
        </div>

        <div class="flex flex-col flex-1 lg:ml-4 mb-4">
          <span class="text-sm mb-2 font-bold">UI/UX</span>
          <ProgressBar @title="Javascript" @value={{60}} />
        </div>
      </div>

      <div class="flex flex-col lg:flex-row flex-1">
        <div class="flex flex-col flex-1 lg:mr-4 mb-4">
          <span class="text-sm mb-2 font-bold">Ember.js</span>
          <ProgressBar @title="Javascript" @value={{95}} />
        </div>

        <div class="flex flex-col flex-1 lg:ml-4 mb-4">
          <span class="text-sm mb-2 font-bold">React / React Native</span>
          <ProgressBar @title="Javascript" @value={{75}} />
        </div>
      </div>
    </div>
  </div>
</div>
